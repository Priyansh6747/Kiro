import { KiroMarketingPage } from "@/components/KiroMarketingPage";

export const metadata = {
  title: "Kiro — Task orchestration, not a to-do list",
  description: "A virtualized timeline, a dependency resolver, and six autonomous agents.",
};

export default function RootPage() {
  return <KiroMarketingPage />;
}
